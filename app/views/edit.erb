<!DOCTYPE html>

<html>
    <body>
        <form action="/articles/<%= @article.id%>" method="post">
        Title: <%= "#{@article.title}" %><input type="text" name="title">
        Content: <%= "#{@article.content}"%> <input type="text" name="content">
        <input id="hidden" type="hidden" name="_method" value="PATCH">
        <input type="submit">
        </form>
    </body>

</html>